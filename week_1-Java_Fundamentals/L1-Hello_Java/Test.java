//* Declare a class with the SAME NAME as the file itself
public class Test {
    // * Typing Convention is in PascalCase
    // Todo: Create "Launcing Point" for Application
    public static void main( String[] args ) {
        // shortcut: "sysout"
        System.out.println("Hello World");
        
        /*
        Todo: Compile file with "javac FileName.java"
        * If errors occurred, compiliing will fail until all syntax errors are corrected
        * After successful compiling, a .class file is made in Bytecode (computer-readable) and we can now invoke the interpreter to execute the program
        Todo: To read code in terminal, run command "java FileName"
        */
    }
}